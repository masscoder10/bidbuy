package com.bidbuy.repository;

import com.bidbuy.entity.BidPackage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BidPackageRepository extends JpaRepository<BidPackage, String> {
}